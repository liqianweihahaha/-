- api:
    def: account_login($identity, $password)
    request:
        # 完整的url路径
        url: $tiger_api_host/tiger/accounts/login
        method: POST
        json:
            identity: $identity
            password: $password
            pid: 65edCTyg

