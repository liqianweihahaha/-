- api:
    name: platform-user-tiger重置用户密码
    def: reset_user_password($user_id, $password)
    request:
        base_url: ${get_platform_tiger_api_host()}
        url: /accounts/$user_id
        json:
            password: $password
