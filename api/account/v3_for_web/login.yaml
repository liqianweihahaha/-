name: 账号密码登录
base_url: ${ENV(tiger_api_host)}
variables:
    - pid: 65edCTyg
request:
    url: /tiger/v3/web/accounts/login
    method: POST
    json:
        identity: $identity
        password: $password
        pid: $pid