- api:
    # name: 解绑第三方账号
    def: unbind_oauth($user_id, $oauth_type)
    request:
        url: $platform_tiger_api_host/accounts/oauth/unbind
        method: POST
        json:
            user_id: $user_id
            oauth_type: $oauth_type
            oauth_version: V3
