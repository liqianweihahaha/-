- api:
    name: 发布作品（body参数为空）
    def: publish_work($work_id, $token)
    request:
        base_url: $tiger_api_host
        url: /tiger/work/$work_id/publish
        method: PUT
        headers:
            Content-Type: $content_type
            Authorization: $token