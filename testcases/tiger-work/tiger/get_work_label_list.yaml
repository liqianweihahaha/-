- config:
    name: 获取作品标签列表（Public）
    base_url: ${tiger_api_host()}

- test:
    name: 获取作品标签列表
    request:
        method: GET
        url: /tiger/work/labels
    validate: 
      - eq: [status_code, 200]

