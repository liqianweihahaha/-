- config:
    name: 获取所有频道作品
    base_url: ${tiger_api_host()}

- test:
    name: 未登录
    request:
        method: GET
        url: /tiger/community/channel/works
    validate:
        - eq: [status_code, 200]