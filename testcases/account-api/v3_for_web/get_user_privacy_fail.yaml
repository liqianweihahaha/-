- config:
    name: 获取用户主账号信息（私密信息）
    base_url: ${tiger_api_host()}

- test:
    name: 未登录
    request:
        url: /tiger/v3/web/accounts/privacy
        method: GET
    validate:
        - eq: [status_code, 403]
        - eq: [content.error_code, E_0]