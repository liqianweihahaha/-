- config:
    name: 获取订单属性
    base_url: ${ENV(order_service_host)}

- test:
    name: 获取订单属性列表
    request:
        method: GET
        url: /orders/attributes   
    validate:
        - eq: [status_code, 200]
        - type_match: [content, object]
        - length_equals: [content.items.0, 5]